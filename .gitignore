# Secrets
telegram_config.json
.env

# Runtime state (régénéré par le bot)
live_state_v33.json
live_state_v33.bak
live_state_v33_*.dbak
live_state_v33.tmp
scan_history_v33.json
live_logs/
incubator_state.json

# Données backtest (trop volumineuses)
backtest_data/
backtest_results/
data_15m_cache/
replay_cache/
sim_cache/

# Scripts d'audit/recherche one-shot
archive/
overnight_report_*.md

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/

# Divers
.agents/
skills-lock.json
*.log

# PRISM V2 — donnees de collecte (volumineuses, regenerables depuis l'API)
prism_v2/data/l2_snapshots/
prism_v2/data/instruments_*.json
prism_v2/data/events/
prism_v2/ledger/discovery_memory.jsonl
# Journal des decouvertes : sortie de run, regenere a chaque passage et
# volumineux (plusieurs Mo). Le code qui le produit est versionne, pas lui.
prism_v2/ledger/discoveries.jsonl
# Observatoire : sortie de collecte, volumineuse et regeneree a chaque session.
# Le code qui la produit et la relit est versionne, pas le flux lui-meme.
prism_v2/data/observatory/
# Historique de funding : sortie de collecte REST, regenerable en minutes.
prism_v2/data/funding/
# Observatoire Polymarket : sortie de collecte.
prism_v2/data/polymarket/
